@model IEnumerable<EduService.Models.Business>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Businesses Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BusniessName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BusniessName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BusniessId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BusniessId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BusniessId })
        </td>
    </tr>
}

</table>
